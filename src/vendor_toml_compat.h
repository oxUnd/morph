/* Forward declarations for vendored toml.c to avoid implicit declaration errors */
#ifndef _POSIX_C_SOURCE
#define _POSIX_C_SOURCE 200809L
#endif
#include <strings.h>
static int parse_millisec(const char *p, const char **endp);
