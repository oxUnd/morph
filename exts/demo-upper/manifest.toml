name = "upper"
version = "1.0.0"
description = "Convert text to uppercase (demo .so ext)"
author = "morph"
type = "so"
entry = "upper.so"
fronts = ["cli"]
categories = ["text", "dev"]
permissions = 0
max_memory_mb = 32
max_cpu_seconds = 5
input_schema = "{\"type\":\"object\",\"properties\":{\"text\":{\"type\":\"string\"}}}"
output_schema = "{\"type\":\"object\",\"properties\":{}}"

[build]
command = "make"
